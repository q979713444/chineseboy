<?xml version="1.0" encoding="utf-8"?>
<!-- view的各种状态(正常无焦点,焦点,onclick按下、放开等等) -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 选中未up时 -->
    <item android:state_pressed="true"><shape>
<solid android:color="#ffffff" />
            <!--
	        	颜色渐变
	        	startColor:开始颜色
	        	endColor:结束颜色
	        	angle:渐变方向 
            -->
            <!--
	        	按钮边缘
	        	width:边缘宽
	        	color:边缘颜色
            -->
            <stroke android:width="2dp" android:color="@color/login_ed1" />
            <!--
	        	按钮四个圆角
	        	radius:半径
            -->
            <corners android:bottomLeftRadius="5px" android:bottomRightRadius="5px" android:topLeftRadius="5px" android:topRightRadius="5px" />

            <!-- 按钮文字和边缘距离(内边距) -->
            <padding android:bottom="5dp" android:left="5dp" android:right="5dp" android:top="5dp" />
        </shape></item>

    <!-- 焦点 -->
    <item android:state_focused="true"><shape>
        <solid android:color="#ffffff" />
         <stroke android:width="2dp" android:color="@color/title_color" />
            <!--
	        	按钮四个圆角
	        	radius:半径

            -->
            <corners android:bottomLeftRadius="5px" android:bottomRightRadius="5px" android:topLeftRadius="5px" android:topRightRadius="5px" />

            <!-- 按钮文字和边缘距离(内边距) -->
            <padding android:bottom="5dp" android:left="5dp" android:right="5dp" android:top="5dp" />
        </shape></item>

    <!-- 无焦点 -->
    <item><shape>
        <solid android:color="#ffffff" />
         <stroke android:width="2dp" android:color="@color/login_ed1" />
            <!--
	        	按钮四个圆角
	        	radius:半径

            -->
            <corners android:bottomLeftRadius="5px" android:bottomRightRadius="5px" android:topLeftRadius="5px" android:topRightRadius="5px" />

            <!-- 按钮文字和边缘距离(内边距) -->
            <padding android:bottom="5dp" android:left="5dp" android:right="5dp" android:top="5dp" />
        </shape></item>

</selector>